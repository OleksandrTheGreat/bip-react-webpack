import * as React from 'react';
import {state} from '../../../shared';
import {FormPage} from '../../common/Form';

export class TransactionPage extends FormPage<any> {

  constructor(props) {
    super(props);

    //this._onSave = this._onSave.bind(this);
  }

  render() {
    return (
      null
    );
  }
}
